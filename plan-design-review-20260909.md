# Catus 디자인 계획 사전 검토 · 2026-09-09

상태: 사전 검토 후 사용자의 “너가 개선해봐” 요청에 따라 활동 선택·모바일 가독성·주요 조작 크기 개선을 구현했다.
후속 구현·실제 캡처·검증은 [디자인 개선 기록](design-polish-20260909.md)을 확인한다.
`plan-design-review`로 작성한 아래 내용은 **구현 전 시점의 관찰 기록**이다. 당시 대기 상태·점수·검증 범위를 현재 상태로 해석하지 않는다.
분리 에셋 지도 기본 전환과 공동 공부 모션은 여전히 남은 과제다.

## 대상과 근거

- 대상: `.`.
- 실행 주소: https://oneorthree.github.io/planning-document/ . PID 57763 서버 유지.
- 기준: 사용자 대화의 최신 결정 → 최신 후속 설계 기록 → 초기 원화/시안 기록.
- 9개 실제 캡처: 데스크톱 마을, 휴대폰 마을, 활동 선택, 공부, 낚시 휴식, 게시판, 오두막, 입항, 상점. IA 전체 구조 추가 캡처.
- 캡처 위치: `이전 로컬 검토 캡처(현재 저장소 미포함)`.
- 휴대폰 검토는 브라우저 390×844 CSS px이며 실제 기기·VoiceOver·네이티브 성능 검증이 아니다.
- 기존 테스트 통과 수치는 기존 기록을 읽은 것이다. 이번 검토에서 전체 기능 테스트를 재실행하지 않았다.

## 초기 판단

설계 완결성 **6/10**. 주관적 사전 평가이며 버그율이나 사용자 실험 점수가 아니다.
섬 중심 화면, 장소별 활동, 로컬 데이터 경계는 구체적이다. 그러나 활동을 발견하는 첫 선택,
글자/터치 크기, 기본 배경의 확대 품질, 공동 공부의 범위, 문서 우선순위가 이식 기준으로 덜 정리되어 있다.
10점의 조건은 시안을 더 많이 만드는 것이 아니라 아래 제안에 대한 결정을 내리고,
각 결정의 성공·빈 상태·오류·복귀·모바일 기준을 하나의 계획에 명시하는 것이다.

## 확인된 사항과 제안

### 1. [제안] 활동별 차이가 IA에서는 보이지만 실제 시작 화면에서는 숨겨진다

- 관찰: IA 전체에는 독서/공부/휴식 원화가 나란히 보인다. 실제 FO-1은 기본값 독서인 드롭다운이다.
  마을 하단은 ‘집중 시작’ 하나여서 처음 온 사용자가 낚시 휴식을 발견하기 어렵다.
- 근거: `activity-choice.png`, `main-phone.png`, `ia-overview.png`.
- 제안: 활동 선택 상단에 독서·공부·휴식의 짧은 선택지 3개를 항상 표시한다.
  원화는 선택한 활동 하나만 크게 보이고 장소·시간 선택·시작 문구를 함께 바꾼다.
  작업은 공부와 같은 장소를 쓰는 세부 태그로 유지한다.
- 원칙: 클릭 수보다 선택을 이해하는 데 드는 생각을 줄인다.
- 후속 상태: 사용자의 개선 요청으로 구현했다. 세 활동 상시 표시, 작업 세부 태그, 키보드 선택, IA 주소 동기화를 검증했다.

### 2. [필수] 모바일 조작 크기가 문서의 기준과 다르다

- 관찰: 실제 건물 시트의 헤더 조작 버튼은 36×44 및 32×44px,
  오두막 기간 선택은 109×36px, 날짜 화살표는 37×44px로 측정됐다.
  이는 프로젝트가 기술한 주요 조작 44px 이상 기준과 맞지 않는다.
- 근거: `notice-phone.png`, `cabin-phone.png`, 브라우저 getBoundingClientRect 측정.
- 제안: 아이콘 그림은 작게 유지해도 버튼의 실제 영역은 최소 44×44px로 지정한다.
  320px에서 헤더 제목과 겹치지 않는지 별도로 검증한다.
- 제한: 이번에는 포인터 타깃의 DOM 경계를 측정했다. 실제 iOS 터치 사용성 테스트는 하지 않았다.

### 3. [제안] 중요한 안내가 9~11px에 몰려 있다

- 관찰: 공지 본문 13px, 건물 설명 10px, 권한 안내 11px, 로컬 저장/미전송 안내 9px.
  오두막 지표 설명 9px. 전체 화면에 넘침이 없다는 것과 편하게 읽힌다는 것은 다르다.
- 근거: `notice-phone.png`, `cabin-phone.png`, 브라우저 getComputedStyle 측정.
- 제안: 읽는 본문 16px, 보조 설명 최소 12px를 기본으로 두고 문장 수와 세로 밀도를 조절한다.
  안전·저장 관련 안내는 장식용 캡션보다 높은 우선순위를 준다.
- 제한: 텍스트 대비 전체 수치 검사는 이번에 하지 않았으므로 접근성 인증/통과를 주장하지 않는다.

### 4. [제안] 배경 에셋 복사와 에셋 조합 적용은 구분해야 한다

- 관찰: 기본 `village-background.js:4`는 여전히 `assets/village-map-cel-v3.png`를 선택한다.
  논리/원화 크기는 1536×1024. 분리 에셋 렌더링은 `village.js:53`의 `?separated=1` 실험 분기다.
  공부 장면에서 선명한 테이블과 확대된 배경의 품질 차이가 보인다.
- 근거: `study-phone.png`, `village-background.js`, `village.js`, `background-refinement-notes.md`.
- 제안: 원화를 단순히 확대해서 품질 문제를 해결했다고 보지 않는다.
  지형/건물/가구/캐릭터별 크기·접지점·가림 순서·클릭 영역·길 진입점 계약을 먼저 확정한다.
  기본 지도, 섬 전체 보기, 상점 미리보기, 입항을 같은 월드 데이터에서 그리도록 계획한다.
- 현재 한계: 분리 모드의 품질·좌표 검증은 완료하지 않았다. 파일이 있다는 이유로 기본 전환을 권하지 않는다.

### 5. [제안] 공동 공부는 아직 한 마리의 공부 장면이다

- 관찰: 실제 공부 장면은 모모 한 마리이며 문구도 ‘빈자리 세 곳 · 혼자서도 시작할 수 있어요’다.
  `activity-implementation-notes.md`도 친구 착석·대화 미구현을 명시한다.
- 근거: `study-phone.png`, `activity-implementation-notes.md:22`.
- 제안: 실제 접속 친구인지 단순 시연 캐릭터인지 먼저 구분하고,
  0~4인 착석·인사·필기·퇴장 상태를 설계한다. 없는 친구를 실시간 참여자로 보이게 만들지 않는다.
  앱 연결 전 로컬 시연을 만들면 ‘시연 친구’로 명시한다.
- 이 검토에서 멀티플레이 또는 새 캐릭터 모션을 구현하지 않았다.

### 6. [제안] 최신 설계 기준을 한 곳에서 찾기 어렵다

- 관찰: `panel-design.md:10`의 올리브색 기준, 26~27행의 잠금 화면 설명은 초기 기록이다.
  최신 `island-theme-fishing-notes.md`, `ia-redesign-notes.md`는 섬 테마·무료 낚싯대·프로토타입 전체 개방을 설명한다.
  적용 범위 내 단일 `DESIGN.md`는 찾지 못했다.
- 제안: 이력을 지우지 말고 ‘초기 시안/현재 기준’ 표식을 붙인다.
  승인 원화, 색/서체/글자 크기/터치 크기, 활동별 상태, 데이터와 모션의 경계를 현재 기준 문서로 연결한다.
  프로토타입 전체 개방을 제품의 영구 해금 정책 확정으로 오해하지 않게 한다.

## 이미 갖춘 것

- 승인된 모모·동물 원화 및 독서/보행 리깅을 재사용할 수 있다.
- 단일 지도 좌표, 모바일 근경/전체 보기, 건물 시트, 고정 시작 버튼이 있다.
- 독서·공부·휴식의 기록 분리, 무료 낚싯대, 세션 복원, 역할별 권한 시연이 있다.
- IA 35화면/9시트, 공유 화면 메타데이터, 로컬/미연결 경계가 문서화되어 있다.
- Jua / Gowun Dodum, 공통 tokens.css, 섬 테마 오버라이드가 있다.
- 기존 정본과 후속 결정이 충돌할 때 후속 사용자 결정의 적용 범위를 유지한다.

## 다음 설계 검토의 범위

7개 검토 축은 정보 구조, 상호작용 상태, 사용자 여정, 획일적인 시안 위험,
디자인 시스템 정합성, 반응형·접근성, 미확정 결정이다.
현재는 사전 진단과 증거 수집까지 진행했다. 각 제안을 승인된 결정으로 간주하거나
7개 검토 축의 개선이 모두 끝났다고 보고하지 않는다.

권장 순서: 활동 발견성 → 모바일 글자/조작 크기 → 에셋 조합 계약 → 공동 공부 상태 → 현재 설계 정본 정리.
상태 검토에서는 로컬 저장 성공/실패, 새 섬의 빈 상태, 활동 중단·앱 복귀,
아트 로드 실패·WebGL 미지원·동작 줄이기를 기존 구현과 대조한다.
실제 서버 동기화 상태와 네이티브 접근성은 별도 계획으로 구분한다.

## 이번 범위 밖

- 실제 앱·서버·OS 차단·결제·멀티플레이 구현.
- 기존 소스 수정, Git stage/commit/push, 기존 사용자 브라우저 데이터 변경.
- 작업 중인 분리 배경 실험을 기본 화면으로 자동 교체.
- 제품 해금/보상/이관 정책을 새로 확정.
- 다른 작업의 `.claude/trouble-log.py`, `.claude/__pycache__/` 변경 정리.

## 시안 생성 상태

스킬의 gstack designer로 활동 선택 개선 이미지 생성을 시도했으나
`No OpenAI API key found`로 실패했다. 키를 검색하거나 설정하지 않았다.
생성·승인된 신규 목업은 0개이며 실제 캡처 10개를 근거로 사용한다.
이미지 생성 실패를 앱 구현 실패 또는 리뷰 완료로 보고하지 않는다.

## GSTACK REVIEW REPORT

| 검토 | 실행 | 상태 | 발견 내용 |
| --- | --- | --- | --- |
| 디자인 계획 사전 검토 | 1 | 개선 방향 승인 대기 | 초기 완결성 6/10, 구체적 관찰 6개, 코드 수정 0개 |
| 7개 상세 검토·계획 확정 | 0 | 미완료 | 첫 활동 선택 제안에 대한 답변 전 |
| 독립 모델 검토 | 0 | 미실행 | 실행·합의가 있었다고 주장하지 않음 |

**VERDICT:** 사전 검토 결과이며 구현 승인이나 최종 디자인 통과가 아니다.

**UNRESOLVED DECISIONS:**
- 활동 3개를 항상 표시하는 선택 구조의 승인.
- 나머지 사전 관찰은 상세 검토에서 결정할 제안이며 아직 승인되지 않음.
