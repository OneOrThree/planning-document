# 공동 수정 규칙

이 저장소는 공개 기획 문서와 로컬 웹 프로토타입의 원본이다. 앱·서버 코드는 별도 `phone` 저장소에 있다.

## 시작

1. `git status --short`로 다른 사람의 변경을 확인하고 보존한다.
2. 기능·문구·화면을 수정할 때 [수정 가이드](docs/editing-guide.md)의 담당 파일과 연결 파일을 먼저 확인한다.
3. 검증·실행·배포를 바꿀 때 [하네스](docs/harness.md)를 읽는다.

## 완료 기준

- 기획의 `decision`(채택 상태)과 `proof`(구현 증거)를 따로 유지한다. 문구 수정만으로 구현 완료를 표시하지 않는다.
- 기능 ID와 장면 ID는 공유 링크다. 삭제·통합 시 제외 이력을 남기고 기존 링크 영향을 확인한다.
- 저니 공통 루프는 `journey-route-data.js`의 공통 객체를 수정한다. 경로별로 같은 내용을 복제하지 않는다.
- 정책 변경은 관련 원문·기능 목록·화면 요약·결정 로그의 충돌을 확인한다. 미정 수치는 제안으로 유지한다.
- 공개 파일에는 개인 PC 경로, 인증 정보, 사용자 데이터, 비공개 앱 소스를 넣지 않는다.
- `npm run check`와 `npm test`를 실행한다. 실행하지 못한 검증은 이유와 함께 보고한다.
- Pages 관련 변경은 `npm run build` 후 `/planning-document/` 하위 경로에서 확인한다.
- 요청에 push·배포가 포함될 때만 외부 반영한다. 수정한 파일만 명시적으로 스테이징한다.

## 리뷰

리뷰·요약·제목은 한국어로 작성한다. 코드 식별자와 오류 원문은 유지한다.
버그·데이터 손실·보안·런타임 크래시는 `[필수]`, 그 외는 `[제안]`으로 표시한다.
포맷·임포트 순서·네이밍 취향은 지적하지 않고, 같은 문제는 대표 위치에서 한 번만 설명한다.
