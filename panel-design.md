# 건물 기능 화면 — 이미지 분석과 구현 방향

2026-09-08 초기 시안 기록. `image-to-code`와 `imagegen` 내장 도구를 사용했다.
현재 구현의 색·활동 선택·모바일 크기는 [2026-09-09 개선 기록](design-polish-20260909.md)을 우선한다.
아래 올리브색·초기 글자 크기·잠금 표현은 현재 화면의 정본이 아니다.
7개 건물마다 독립적인 1024×1536 기능 화면 시안을 생성하고 분석한 후 구현했다.
이미지들은 UI 설계 참고이며 런타임의 지도 이미지를 대체하지 않는다.

## 공통 추출

- 시안의 상단 22~35%는 마을, 아래는 아이보리 기능 시트다. 기존 마을 카메라와 리깅을 그대로 유지한다.
- 단색 아이보리 #fbf8ee, 올리브 #55684a, 진한 코코아 #3e3b31. 게시판 메모는 버터색, 우체국은 옅은 살구, 항구는 옅은 청록을 작은 면적으로 쓴다.
- 시트의 제목은 둥근 한국어 디스플레이 24px, 기능 헤드라인 27~30px, 본문 14px/1.7. 숫자는 별도 산세리프 56~68px, 단위 18px.
- 헤더·본문·고정 하단 버튼을 구분한다. 본문만 스크롤. 좌우 26~28px 여백, 큰 섹션 사이 24~28px, 머리글과 설명 사이 6~10px.
- 밑줄형 탭, 가는 구분선, 네모에 가까운 둥근 14~18px 버튼을 반복한다. 전체 시트 외에는 필요한 메모·선택 항목만 감싼다.
- 건물 헤더에는 기존 아이콘 계열의 선 아이콘을 쓰고, 새로 생성한 서로 다른 건물 삽화를 실제 지도 대신 넣지 않는다.
- 데스크톱은 오른쪽 넓은 패널, 모바일은 지도 위의 높이 조절 가능한 시트. 모바일에서도 주 버튼이 화면 아래로 밀리지 않게 한다.

## 건물별 시안 분석

| 시안 | 중심과 읽는 순서 | 구현할 동작 |
| --- | --- | --- |
| [모닥불](assets/panel-references/camp.png) | 한 문장 헤드라인 → 얇은 원호의 큰 25분 → 세 시간 선택 → 친구들 → 집중 제한 안내 → 시작 | 시간 선택 시 숫자·버튼 문구 함께 갱신, 기존 세션으로 연결 |
| [마을회관](assets/panel-references/town.png) | 섬 이름과 새싹 → Lv.3와 62% 진행 → 세 단계 성장 길 → 섬의 약속 → 설정 | 열린/다음/이후 건물 구분, 이름 설정. 미확정 임계값은 확정 수치로 만들지 않음 |
| [오두막](assets/panel-references/cabin.png) | 밑줄 탭 → 큰 집중 시간 → 7개 막대 → 주간 합계 → 친구 목록 | 개인/섬 전환, 날짜별 막대 선택. 시안의 미래 요일 데이터 대신 최근 7일 사용 |
| [게시판](assets/panel-references/board.png) | 탭 → 핀 메모 한 장 → 세 퀘스트 열린 행 → 이어하기 | 공지/약속 탭, 퀘스트 상세와 자료 공유 안내. 서버 전송 없음 |
| [항구](assets/panel-references/harbor.png) | 질문 → 시간 귀속 안내 → 내 섬/구경 → 현재 섬과 목적지 → 이동 | 선택과 이동을 분리, 현재 섬으로 중복 이동 방지, 방문 읽기 전용 유지 |
| [전망대](assets/panel-references/tower.png) | 짧은 기대 문장 → 잠금 안내 → 미리보기 탭 → 예시 순위 → 성장 | 두 범위의 예시만 전환. 잠긴 건물을 실제 해금하지 않음 |
| [우체국](assets/panel-references/post.png) | 한 문장 → 봉투와 잎 봉인 → 잠금 → 프리셋 문구 → 성장 | 문구 선택 시 봉투 미리보기, 실제 편지 전송 없음 |

시안의 새로운 고양이 아바타는 재사용하지 않는다. 이미 승인된 모모 원화를 그대로 쓴다.
텍스트·아이콘·원호·차트·봉투는 접근 가능한 실제 HTML/SVG/CSS UI로 구현한다.
실제 데이터·해금 수준·소통 서버를 새로 연결하는 작업은 범위 밖이다.

## 산출물

시안: `assets/panel-references/{camp,town,cabin,board,harbor,tower,post}.png`.
전체 내장 생성 프롬프트: `assets/panel-references/prompts.json`.

## 실제 구현에서 조정한 점

- 모바일 기본 시트에서는 모닥불 헤드라인과 숫자 원호를 나란히 배치한다. 390×844에서 시간 선택 세 개가 스크롤 없이 보이며, 확장하면 시안의 중앙 정렬 구성으로 돌아간다.
- 시안의 새 고양이 대신 승인된 모모 원화를 작은 프로필로 재사용했다. 마을의 걷기·독서 리그는 유지했다.
- 오두막은 날짜가 실제 최근 7일과 맞도록 계산한다. 막대 선택, 키보드 탭 전환, 종료한 세션 반영이 동작한다.
- 항구의 선택과 이동을 분리했고 이름·레벨·친구 수를 현재 섬에 맞췄다. 집중 기록은 섬 ID로 구분하여 이름 변경과 이동에도 귀속이 유지된다.
- 책 읽기 결과는 영수증형 화면, 방문 중 건물은 읽기 전용 안내 화면으로 같은 여백·타이포 규칙을 적용했다.
- 모바일 손잡이는 탭·스와이프, 탭 메뉴는 방향키·Home·End, 닫기는 Escape와 원래 버튼 포커스 복원을 지원한다.

실제 구현 스크린샷: `screenshots/panels/` 안의 건물별 `*-desktop.png`, `*-mobile.png`.
시안 이미지는 런타임 화면에 배경으로 깔지 않는다. 텍스트·선택 상태·차트·주요 버튼은 실제 DOM이다.

## 검증 결과

`node verify-panels.cjs`: 66개 통과. 320×640, 390×844, 768×844, 1440×1000의
7개 건물 레이아웃과 고정 버튼, 탭·차트·시간 선택, 이름 변경, 섬별 기록 귀속,
읽기 전용 방문, 잠금·프리셋, 키보드 포커스, 모바일 탭·스와이프를 검사했다.
기존 독서 리그 26개와 걷기·섬 연결 41개도 회귀 검증했다.
