# 책 읽는 마을 친구 후보

2026-09-08 사용자 요청: 강아지·카피바라·레서판다를 각각 생성해 `rig.html`에 추가. 이후 골든 리트리버를 **추가** 요청했다. 모모와 앞선 강아지를 교체하지 않는다.

내장 `imagegen` 사용. 기준은 사용자가 승인한 `assets/momo-rig-source.png`다.
단일 캐릭터, 같은 초록 책과 금색 책갈피, 3/4 앉은 자세, 눈을 뜬 얼굴,
저채도 크림·갈색·올리브, 깔끔한 윤곽과 단순화한 음영으로 맞췄다.

- 강아지: 크림·캐러멜 털, 긴 늘어진 귀. 첫 번째 강아지 후보로 보존.
- 골든 리트리버: 넓은 주둥이·큰 검은 코·꿀빛 털·짧게 접힌 귀. 추가 후보.
- 카피바라: 넓은 주둥이·작은 둥근 귀·황갈색 털. 꼬리 없는 둥근 몸.
- 레서판다: 흰 눈썹·뺨, 짙은 팔다리, 붉은 갈색 고리 꼬리.

전체 최초 생성 프롬프트: `assets/reading-friends-prompts.json`.
처음 3개 생성본은 체크무늬가 이미지에 들어가 실제 알파가 없었으므로 그대로 런타임에 쓰지 않는다. 원화를 보존한 투명 배경 추출본을 별도로 만든다.

`rig.html` 상단에 캐릭터 선택 드롭다운과 다섯 원화 썸네일을 함께 표시한다. 선택은 URL에 보존하며 두 조작을 동기화한다. 썸네일은 JavaScript 초기화와 무관하게 HTML에서 볼 수 있다.

2026-09-08 후속 작업에서 강아지·카피바라·레서판다를 각각 Astra 에이전트가 맡아 투명 원화와 독립 관절 프로파일로 전환했다. 모모의 눈 텍스처를 다른 종의 얼굴에 섞지 않는다. 열린 원화의 알파를 유지하며 같은 캐릭터의 감은 눈 원화에서 눈 영역만 합성한다. 감은 눈 보조 파일은 투명 출력이 아니어도 배경을 렌더링하지 않는다.

| 캐릭터 | 실제 투명 원화 | 전용 프로파일 | 관절 |
| --- | --- | --- | --- |
| 강아지 | `assets/puppy-reading-rig-v2.png` | `reading-profile-puppy.js` | 12 |
| 골든 리트리버 | `assets/golden-retriever-rig-source.png` | `reading-profile-golden.js` | 12 |
| 카피바라 | `assets/capybara-reading-rig-v2.png` | `reading-profile-capybara.js` | 8 |
| 레서판다 | `assets/red-panda-reading-rig-v2.png` | `reading-profile-red-panda.js` | 12 |

생성 모드는 내장 `imagegen`이다. 편집 프롬프트·원본 생성 경로·알파 측정·좌표 근거는 `puppy-rig-work.md`, `golden-rig-work.md`, `capybara-rig-work.md`, `red-panda-rig-work.md`에 각각 기록한다. 원본 후보 이미지는 덮어쓰지 않았다. 골든 리트리버도 별도 Astra 에이전트가 12관절·자체 감은 눈·책장 넘기기를 완성했다.

공통 `momo-rig.js`가 종별 관절·가중치 영역·모션·눈·책 좌표를 받아 렌더링한다. 책 읽기/쉬기/고개 끄덕, 속도, 타임라인, 수동 관절, 원화 비교, PNG 및 프로파일 포함 JSON 내보내기를 제공한다. JSON은 이 프로젝트의 WebGL 메시 형식이며 Spine/Rive 파일은 아니다.

책장에 관한 사용자 피드백 후 좁은 종이면 클립을 위쪽 공간까지 열고, 표지 윗선 아래만 가리도록 변경했다. 4px 리본 대신 곡면 종이를 렌더링하고, 들린 종이도 고개가 아닌 책 관절에 고정한다. 강아지는 `lift:55`, `depth:44`로 조절했다. 눈 감김은 위 눈꺼풀이 내려오는 방향이며 중간 프레임도 별도로 검증한다.

검증: `verify-animal-animation.cjs`(통합), `verify-reading-friends.cjs`(선택·내보내기), 캐릭터별 `verify-*-rig.cjs`(관절·가중치·시각 상태). 기존 모모는 `verify-momo.cjs`로 별도 회귀검증한다.

이 단계에서 승인된 섬의 모모를 자동으로 교체하지 않는다.

## 캐릭터별 산책 작업실

`walk.html`에서 모모·강아지·카피바라·레서판다를 선택한다. 새 세 종은 각각 Astra 에이전트가 앞뒤 투명 원화와 독립 관절·가중치·보폭을 맡았다. 앞뒤 원화는 각 1254×1254 실제 알파 PNG이며 체크무늬를 배경에 그리지 않는다. 골든 리트리버의 독서 리그는 유지하되 산책 리그는 이번 범위에 포함하지 않았다.

| 캐릭터 | 앞뒤 원화 | 전용 프로파일 | 관절 | 생성·검증 기록 |
| --- | --- | --- | --- | --- |
| 강아지 | `assets/puppy-walk-{front,back}-v1.png` | `walk-profile-puppy.js` | 20 | `puppy-walk-work.md` |
| 카피바라 | `assets/capybara-walk-{front,back}-v1.png` | `walk-profile-capybara.js` | 18 | `capybara-walk-work.md` |
| 레서판다 | `assets/red-panda-walk-{front,back}-v1.png` | `walk-profile-red-panda.js` | 20 | `red-panda-walk-work.md` |

공통 `momo-walk.js`에 렌더링을 두고 원화 좌표·관절·영역 가중치·보행 진폭은 프로파일별로 분리한다. 카피바라에 꼬리 관절을 만들지 않는다. 모모용 색 보정을 새 친구에게 적용하지 않으며 다시 모모를 선택하면 기존 보정으로 복원한다. 앞뒤 소스별 2,401정점·4,608삼각형을 사용한다.

레서판다는 독서 원화에 맞춘 자체 앞뒤 표시 색 보정을 프로파일의 `palette`에 저장한다. 원본 PNG는 유지하며 WebGL 캔버스와 대체 이미지에 같은 보정을 적용한다. 모모의 보정을 공유하지 않는다. 앞뒤 각 65개 보행 위상에서 메시 뒤집힘 0을 확인했다.

드롭다운과 썸네일 선택, 네 방향, 걷기/대기, 재생·속도·타임라인, 수동 관절, 전용 프로파일을 포함한 JSON 다운로드를 연결했다. 독서/산책 작업실 사이에서 지원하는 캐릭터의 선택을 유지한다. 새 친구의 섬 배치는 아직 하지 않았다.

통합 검증은 `verify-walking-friends.cjs`, 각 종의 메시·접지·시각 검증은 `verify-puppy-walk.cjs`, `verify-capybara-walk.cjs`, `red-panda-walk-qa.cjs`를 사용한다. 기존 모모의 산책·섬 연결은 `verify-walk.cjs`로 회귀검증한다.

## 앱 이식 경계

사용자와 합의한 순서는 웹 프로토타입 완성 후 앱 이식이다. 원화·관절·가중치·모션과 경로 계산은 재사용 가능한 재료이며 HTML/DOM·브라우저 입력·렌더러 생명주기는 네이티브에 맞춰 옮겨야 한다. 현재 JSON은 Spine/Rive 호환 포맷이 아니다. 앱 이식 시 한 캐릭터로 실기기 렌더링·프레임률·메모리·발열·백그라운드 복귀를 먼저 검증한다. 실제 앱 코드는 이번 작업에서 변경하지 않았다.

## 걷기·독서 색 통일

걷기 앞·뒤 원화의 주황색 채도와 갈색 명암이 독서 원화와 달랐다. `momo-walk.css`의 시점별 표시 색 보정으로 독서 원화에 맞췄다. WebGL 캔버스와 대체 이미지 모두 같은 보정을 적용하며 원본 이미지·관절·알파는 보존한다. 검증 및 실제 렌더 비교는 `verify-walk.cjs`, `screenshots/momo-color-runtime.png`에 있다.

집중 화면의 종이색 페이드는 배경 이미지 위, 리깅 캐릭터 아래에 두어 털색을 덮지 않도록 한다.
