# 같이숲 v2 화면 에셋

2026-09-15 기준 v2 전체 화면 모음과 집중 프로토타입(낚시섬 집중 → 휴식 → 다시 집중)에 실제로 들어간 그림 103개와, 딸기 소다 UI 킷 기본(색·글자 규칙과 공통 컨트롤 10종)이다. Jira: GROMO-1887

- 그림 목록 기준: 두 결과물을 다시 만들면서 실제로 열린 그림 파일을 기록하고, 완성된 화면에 들어간 그림만 남겼다. 소리·설정 파일과 화면에 쓰이지 않는 그림은 넣지 않았다.
- 폴더 구조는 원본 작업 폴더 `gachisup-R61-assets/assets/`와 같다. 경로 앞부분만 `assets/gachisup-r61/`로 바뀐다.
- 원본을 그대로 복사했다. 크기·색·투명도는 바꾸지 않았다. 모두 낮 그림이다(UI 킷용 밤 섬 1장 제외).
- 사이트 첫 화면(디자인 작업실, 저장소 루트 `index.html`)에서 그림을 한눈에 볼 수 있다.
- 생성 원본과 프롬프트는 원본 작업 폴더의 `_source/`에 있고 이 저장소에는 올리지 않았다.

## v2 화면 그림 (103개)

| 폴더 | 개수 | 내용 |
| --- | --- | --- |
| `backgrounds/island/` | 4 | 우리 섬(마을섬) 확정 6번. 건물 없는 섬, 마을회관 단계, 게시판 단계, 모든 건물 완공. 1536 × 1024 |
| `backgrounds/fishing-island/` | 1 | 집중하는 낚시섬(연못·개울·해변). 1254 × 1254 |
| `buildings/` | 9 | 마을회관·게시판·축음기·도서관·우체통·전망대·상점 7채, 도서관 실내(원형 테이블 방·일기장 한 쪽) 2장 |
| `boats/raft/` | 5 | 기본 뗏목, 뗏목 앞뒤 레이어, 옆모습 항해 앞뒤 레이어 |
| `characters/cat/` | 71 | 6색 낚시·끌어올리기 각 4프레임과 아바타, 검정 걷기 6·눈 깜빡임 3·옆모습·앉기, 기본 서기, 책 읽기(검정·회색) |
| `characters/dog/npc/`, `characters/parrot/npc/` | 2 | 상점 강아지, 안내 앵무새 |
| `props/fishing/`, `props/boat-lantern/` | 6 | 낚싯대, 물고기, 잡은 물고기 한 마리·작은 더미·큰 더미, 배 등불 |
| `ui/emotes/` | 5 | 집중 이모티콘 인사·응원·졸림·웃음·하트뿅뿅 |

## UI 킷 · 딸기 소다 기본 (`ui-kit/`)

시작 페이지는 `ui-kit/foundations/index.html`이다.

- 넣은 것: 공유 규칙 파일 `ui-kit/tokens.css`·`strawberry.css`·`tokens.json`과 `ui-kit/foundations/` 전체(색과 글자, 버튼, 칩과 배지, 진행과 시간, 카드와 목록, 캐릭터와 아바타, 확인 모달, 바텀시트, 입력과 컨트롤, 피드백과 빈 상태).
- 뺀 것: 화면 컴포넌트 15종과 전체 킷 첫 페이지. 옛 정책 화면(배 위 집중, 꽃나팔 방송기 등)이 섞여 있고 소리·옛 에셋 연결이 필요하다.
- UI 킷이 쓰는 파일 5개를 함께 넣었다: `backgrounds/island/base/night.png`, `characters/cat/black/poses/guitar.png`·`reading.png`, `characters/dog/idle.png`, `fonts/gowun-dodum.ttf`(고운돋움).
- 원본에서 바꾼 점: 그림·폰트 경로에서 `assets/` 한 단계만 뺐다(이 폴더에서는 그림이 UI 킷 바로 옆에 있다). 공통 컨트롤 페이지의 UI 킷 첫 페이지 링크는 `foundations/index.html`로 돌렸다.
- 공통 컨트롤 안에서 화면 컴포넌트(우체통·공지·섬 홈·내 모습)로 가는 예시 링크는 이 묶음에 없어 열리지 않는다.
