# 작성·통계·로컬 완결 흐름

2026-09-08. 실제 앱/서버 코드는 조회만 했다. 구현 대상은 로컬 Catus 프로토타입이다.

## 근거와 승계

- `app/app-dev/src/screens/StatsScreen.tsx`: 일·주·월, 직전 비교, 목표 달성 달력, 과목별 비율, 일/주 타임테이블, 월별 주차, 최장 세션.
- `app/app-dev/src/types/dto/stats.ts`: 집중·스크린타임·스트릭·히트맵·과목·평균 DTO. 분 단위, 데이터 없음과 0을 구분.
- `app/app-dev/src/screens/stats/LongestSessionStat.tsx`: 종료 시점으로 기간 귀속. 최장 세션은 종료-시작, 방해 시간 미차감.
- `server/data-api/src/main/java/com/oneorthree/phone/group/dto/CreateAnnouncementRequest.java`: 제목 100자, 제목/내용 공백 불가, 서버가 작성자와 권한 판정.
- `server/data-api/src/main/java/com/oneorthree/phone/group/dto/CreateChallengeRequest.java`: 집중/스크린타임 × 하루총량/시간대, 반복 요일, 카테고리 상한, 자정 걸침 불가, 내기는 생성 시 결정.
- 원본 저장소 `references/legacy-design/policy.md` §3–8: 공지·퀘스트는 그룹장, 자료·댓글은 멤버, 신고 필수, 섬별 집중 귀속, 편지 프리셋, 콕찌르기 1일 1회, 섬 순위는 1인 평균.

## 시안 분석

내장 `imagegen` → `image-to-code` 순서로 새로 생성했다.

- `assets/panel-references/board-compose.png`: 아이보리 단일 면, 상단 뒤로/제목/저장 상태, 24px 헤드라인, 14px 라벨, 밑줄형 제목·큰 본문 입력, 고정 체크, 하단 미리보기+게시. 넓은 본문 영역과 명확한 필드 라벨을 코드에 유지한다. 공지·자료·퀘스트 작성이 같은 폼 계열을 쓴다.
- `assets/panel-references/cabin-full.png`: 범위 밑줄 탭 → 기간 선택 → 날짜 이동 → 큰 합계/비교 → 7개 막대 → 3개 핵심 지표 → 상세 탭 → 과목/세션. 제목 24px, 합계 48px, 본문 13–14px. 카드 중첩 없이 선으로 구분한다. 실제 날짜·합계는 모델로 계산하며 시안의 수치와 미래 기록은 그대로 복사하지 않는다.

두 시안의 공통 색은 종이 #fbf8ee, 올리브 #55684a, 잉크 #3e3b31. 모바일 입력은 16px로 키보드 자동 확대를 피하고, 확장 시트에서 작성한다. 기존 마을·승인 고양이 원화는 바꾸지 않는다.

추가 요청: 집중 화면은 `assets/panel-references/focus-session.png`로 별도 생성했다. 작은 타이머 상자를 없애고, 상단 종이색 영역의 큰 76px 타이머·일감·얇은 진행선, 중앙 모닥불/기존 리그, 하단 친구·소리·마치기로 구성한다. 상하 마스크는 CSS 그라데이션이며 지도와 리그는 동일한 원본이다. 원화 속 숫자의 분/초 오차는 복사하지 않고 실제 경과 시간으로 계산한다.

추가 요청: 모든 건물을 기본 개방한다. 전망대·우체국의 레벨 잠금과 체험 진입 게이트는 제거한다. 이는 프로토타입 검토를 위한 개방이며 제품의 해금 수준 확정이 아니다.

## 완료된 기능과 검증

공지·자료 작성은 자동 텍스트 임시저장 → 미리보기 → 수정 → 게시 → 상세 → 댓글·신고·수정·삭제 확인으로 이어진다. 공백·길이와 역할을 모델에서도 검증한다. 공지 고정은 섬마다 하나다. 퀘스트는 카테고리·하루/시간대·요일·목표를 지정하고 참여/취소/종료할 수 있다. 그룹장/그룹원은 마을회관 시연 설정에서 바꾼다.

통계는 같은 세션 배열에서 합계·일별 차트·과목·최장 세션·목표 달력·JSON을 계산한다. KST 월요일 기준이며 미래 날짜는 선택할 수 없다. 개인 기록은 내 모든 섬, 그룹 기록은 해당 섬에 귀속한다. 비공개 친구의 세션은 그룹 합계·목록·내보내기에서도 제외한다. 친구 평균은 현재 섬에서 해당 기간 공개 집중 기록이 있는 친구만 포함하고 나를 제외한다. 섬 간 평균 순위는 공개 멤버 수를 분모로 한다. 두 평균은 서로 다른 기준을 UI에 명시한다.

예시 기록을 끄면 직접 종료한 세션만 남는다. 기기 사용 데이터가 없으면 0분으로 표시하지 않는다. 프리셋 편지·수신 설정은 로컬 저장하며, 오늘 이 섬의 공개 집중 기록이 없는 친구에게 하루 한 번 콕찌르기를 남길 수 있다. 모두 실제 전송이 없음을 표시한다.

QA에서 비공개 친구 세션 노출, 첨부 검증 완료 전 게시 race, 미리보기 복귀 시 파일명 누락, 중복 신고 오류가 열린 다이얼로그 뒤에 표시되는 문제, 우체국 라디오의 전역 입력 폭 상속, 집중 시간 변경 시 선택 일감 초기화를 수정했다. 집중 마스크는 지도 레이어에서 캐릭터 뒤에 위치시켜 승인된 털색을 덮지 않는다.

`verify-store.cjs` 42개, `verify-features.cjs` 40개, `verify-panels.cjs` 62개 통과. PDF는 새로고침 후 다운로드 원본 바이트 일치까지 확인했다. PNG/JPG/WebP 시그니처 지원, 10MB·3개 제한과 위조 PNG 거부, 느린 첨부 검증 중 이중 제출, 모바일 임시저장과 긴 글 줄바꿈, 권한·삭제 취소·신고 중복, 예시 토글·비공개 내보내기를 실제 격리 브라우저에서 검사했다. 건물 패널은 320/390/768/1440px에서 검사한다. 기존 사용자 로컬 데이터는 변경하지 않았다.

## 경계

글·댓글·설정·세션은 이 브라우저의 로컬 저장소에 저장한다. 첨부는 IndexedDB에 보관하고 외부 업로드하지 않는다. 로컬 작성은 실제 공개 게시가 아니므로 콘텐츠 검열을 통과했다고 표시하지 않는다. 서버 연동 시 이미지/텍스트 fail-closed 검열, 권한 판정, 신고 전달, 파일 수명·용량 정책이 필요하다.

스크린타임은 기기 권한이 없어 실제 측정하지 않는다. 예시 데이터와 직접 쌓은 집중 기록을 분리하고 예시 포함 여부를 선택할 수 있게 한다. 앱별 사용량은 서버에 없으므로 임의로 만들지 않는다. 미확정 해금 레벨·성장/보상 단가를 확정하지 않으며 내기 에스크로·푸시·외부 전송은 로컬 UI만으로 완료했다고 주장하지 않는다.
